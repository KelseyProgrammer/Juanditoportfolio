/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // swap in real remote hosts once photos are hosted (e.g. Cloudinary, S3, Contentful)
    remotePatterns: []
  }
};

export default nextConfig;
